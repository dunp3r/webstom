  lottie.loadAnimation({
    container: document.getElementById('lottie-animation'), // el contenedor
    renderer: 'canvas',
    loop: true,
    autoplay: true,
    path: '/assets/animation.json' 
  });

